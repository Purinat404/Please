-- MPro RED v1.0 - Part 2/10
-- GUI Main + M Button

-- ป้องกันรันซ้ำ
pcall(function()
    local old = LP:WaitForChild("PlayerGui"):FindFirstChild("MProRedUI")
    if old then old:Destroy() end
end)

-- สร้าง ScreenGui
local gui = Instance.new("ScreenGui")
gui.Name = "MProRedUI"
gui.ResetOnSpawn = false
gui.DisplayOrder = 999
gui.Parent = LP:WaitForChild("PlayerGui")

-- ปุ่ม M ลอย
local btn = Instance.new("TextButton")
btn.Size = UDim2.new(0, 55, 0, 55)
btn.Position = UDim2.new(1, -75, 0, 20)
btn.BackgroundColor3 = Color3.fromRGB(12, 5, 5)
btn.BorderSizePixel = 0
btn.Text = "M"
btn.TextColor3 = Color3.fromRGB(255, 50, 50)
btn.TextSize = 26
btn.Font = Enum.Font.GothamBlack
btn.AutoButtonColor = false
btn.Parent = gui

local bc = Instance.new("UICorner")
bc.CornerRadius = UDim.new(0, 14)
bc.Parent = btn

local bs = Instance.new("UIStroke")
bs.Thickness = 2
bs.Color = Color3.fromRGB(255, 50, 50)
bs.Parent = btn

-- เอฟเฟกต์เรืองแสงปุ่ม
local glow = Instance.new("UIStroke")
glow.Thickness = 4
glow.Color = Color3.fromRGB(255, 50, 50)
glow.Transparency = 0.7
glow.Parent = btn

-- เมนูหลัก
local menu = Instance.new("Frame")
menu.Size = UDim2.new(0, 300, 0, 400)
menu.Position = UDim2.new(1, -320, 0, 80)
menu.BackgroundColor3 = Color3.fromRGB(12, 5, 5)
menu.BackgroundTransparency = 0.05
menu.BorderSizePixel = 0
menu.Visible = false
menu.Parent = gui

local mc = Instance.new("UICorner")
mc.CornerRadius = UDim.new(0, 14)
mc.Parent = menu

local ms = Instance.new("UIStroke")
ms.Thickness = 2
ms.Color = Color3.fromRGB(255, 50, 50)
ms.Parent = menu

-- Header
local h = Instance.new("Frame")
h.Size = UDim2.new(1, 0, 0, 44)
h.BackgroundColor3 = Color3.fromRGB(20, 8, 8)
h.BorderSizePixel = 0
h.Parent = menu

local hc = Instance.new("UICorner")
hc.CornerRadius = UDim.new(0, 14)
hc.Parent = h

local hFix = Instance.new("Frame")
hFix.Size = UDim2.new(1, 0, 0, 15)
hFix.Position = UDim2.new(0, 0, 1, -15)
hFix.BackgroundColor3 = Color3.fromRGB(20, 8, 8)
hFix.BorderSizePixel = 0
hFix.Parent = h

local ht = Instance.new("TextLabel")
ht.Size = UDim2.new(1, -50, 1, 0)
ht.Position = UDim2.new(0, 15, 0, 0)
ht.BackgroundTransparency = 1
ht.Text = "M-PRO RED"
ht.TextColor3 = Color3.fromRGB(255, 50, 50)
ht.TextSize = 16
ht.Font = Enum.Font.GothamBold
ht.TextXAlignment = Enum.TextXAlignment.Left
ht.Parent = h

-- ปุ่ม X ปิด
local xb = Instance.new("TextButton")
xb.Size = UDim2.new(0, 28, 0, 28)
xb.Position = UDim2.new(1, -36, 0, 8)
xb.BackgroundColor3 = Color3.fromRGB(40, 15, 15)
xb.BorderSizePixel = 0
xb.Text = "X"
xb.TextColor3 = Color3.fromRGB(255, 100, 100)
xb.TextSize = 14
xb.Font = Enum.Font.GothamBold
xb.Parent = h

local xc = Instance.new("UICorner")
xc.CornerRadius = UDim.new(0, 8)
xc.Parent = xb

-- พื้นที่ Scroll
local sc = Instance.new("ScrollingFrame")
sc.Size = UDim2.new(1, -16, 1, -54)
sc.Position = UDim2.new(0, 8, 0, 50)
sc.BackgroundTransparency = 1
sc.BorderSizePixel = 0
sc.ScrollBarThickness = 3
sc.ScrollBarImageColor3 = Color3.fromRGB(255, 50, 50)
sc.CanvasSize = UDim2.new(0, 0, 0, 0)
sc.AutomaticCanvasSize = Enum.AutomaticSize.Y
sc.Parent = menu

local ll = Instance.new("UIListLayout")
ll.SortOrder = Enum.SortOrder.LayoutOrder
ll.Padding = UDim.new(0, 6)
ll.Parent = sc
-- MPro RED v1.0 - Part 2/10
-- GUI Main + M Button

pcall(function()
    local old = LP:WaitForChild("PlayerGui"):FindFirstChild("MProRedUI")
    if old then old:Destroy() end
end)

local gui = Instance.new("ScreenGui")
gui.Name = "MProRedUI"
gui.ResetOnSpawn = false
gui.DisplayOrder = 999
gui.Parent = LP:WaitForChild("PlayerGui")

-- ปุ่ม M ลอย
local btn = Instance.new("TextButton")
btn.Size = UDim2.new(0, 55, 0, 55)
btn.Position = UDim2.new(1, -75, 0, 20)
btn.BackgroundColor3 = Color3.fromRGB(12, 5, 5)
btn.BorderSizePixel = 0
btn.Text = "M"
btn.TextColor3 = Color3.fromRGB(255, 50, 50)
btn.TextSize = 26
btn.Font = Enum.Font.GothamBlack
btn.AutoButtonColor = false
btn.Parent = gui

local bc = Instance.new("UICorner")
bc.CornerRadius = UDim.new(0, 14)
bc.Parent = btn

local bs = Instance.new("UIStroke")
bs.Thickness = 2
bs.Color = Color3.fromRGB(255, 50, 50)
bs.Parent = btn

local glow = Instance.new("UIStroke")
glow.Thickness = 4
glow.Color = Color3.fromRGB(255, 50, 50)
glow.Transparency = 0.7
glow.Parent = btn

-- เมนูหลัก
local menu = Instance.new("Frame")
menu.Size = UDim2.new(0, 300, 0, 400)
menu.Position = UDim2.new(1, -320, 0, 80)
menu.BackgroundColor3 = Color3.fromRGB(12, 5, 5)
menu.BackgroundTransparency = 0.05
menu.BorderSizePixel = 0
menu.Visible = false
menu.Parent = gui

local mc = Instance.new("UICorner")
mc.CornerRadius = UDim.new(0, 14)
mc.Parent = menu

local ms = Instance.new("UIStroke")
ms.Thickness = 2
ms.Color = Color3.fromRGB(255, 50, 50)
ms.Parent = menu

-- Header
local h = Instance.new("Frame")
h.Size = UDim2.new(1, 0, 0, 44)
h.BackgroundColor3 = Color3.fromRGB(20, 8, 8)
h.BorderSizePixel = 0
h.Parent = menu

local hc = Instance.new("UICorner")
hc.CornerRadius = UDim.new(0, 14)
hc.Parent = h

local hFix = Instance.new("Frame")
hFix.Size = UDim2.new(1, 0, 0, 15)
hFix.Position = UDim2.new(0, 0, 1, -15)
hFix.BackgroundColor3 = Color3.fromRGB(20, 8, 8)
hFix.BorderSizePixel = 0
hFix.Parent = h

local ht = Instance.new("TextLabel")
ht.Size = UDim2.new(1, -50, 1, 0)
ht.Position = UDim2.new(0, 15, 0, 0)
ht.BackgroundTransparency = 1
ht.Text = "M-PRO RED"
ht.TextColor3 = Color3.fromRGB(255, 50, 50)
ht.TextSize = 16
ht.Font = Enum.Font.GothamBold
ht.TextXAlignment = Enum.TextXAlignment.Left
ht.Parent = h

-- ปุ่ม X ปิด
local xb = Instance.new("TextButton")
xb.Size = UDim2.new(0, 28, 0, 28)
xb.Position = UDim2.new(1, -36, 0, 8)
xb.BackgroundColor3 = Color3.fromRGB(40, 15, 15)
xb.BorderSizePixel = 0
xb.Text = "X"
xb.TextColor3 = Color3.fromRGB(255, 100, 100)
xb.TextSize = 14
xb.Font = Enum.Font.GothamBold
xb.Parent = h

local xc = Instance.new("UICorner")
xc.CornerRadius = UDim.new(0, 8)
xc.Parent = xb

-- พื้นที่ Scroll
local sc = Instance.new("ScrollingFrame")
sc.Size = UDim2.new(1, -16, 1, -54)
sc.Position = UDim2.new(0, 8, 0, 50)
sc.BackgroundTransparency = 1
sc.BorderSizePixel = 0
sc.ScrollBarThickness = 3
sc.ScrollBarImageColor3 = Color3.fromRGB(255, 50, 50)
sc.CanvasSize = UDim2.new(0, 0, 0, 0)
sc.AutomaticCanvasSize = Enum.AutomaticSize.Y
sc.Parent = menu

local ll = Instance.new("UIListLayout")
ll.SortOrder = Enum.SortOrder.LayoutOrder
ll.Padding = UDim.new(0, 6)
ll.Parent = sc
-- MPro RED v1.0 - Part 3/10
-- Toggle Function

local function makeToggle(parent, text, default, callback)
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, -8, 0, 36)
    row.BackgroundColor3 = Color3.fromRGB(20, 8, 8)
    row.BorderSizePixel = 0
    row.Parent = parent

    local rc = Instance.new("UICorner")
    rc.CornerRadius = UDim.new(0, 8)
    rc.Parent = row

    local rs = Instance.new("UIStroke")
    rs.Thickness = 1
    rs.Color = Color3.fromRGB(80, 20, 20)
    rs.Transparency = 0.5
    rs.Parent = row

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -70, 1, 0)
    label.Position = UDim2.new(0, 12, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(230, 200, 200)
    label.TextSize = 13
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = row

    local toggle = Instance.new("TextButton")
    toggle.Size = UDim2.new(0, 44, 0, 22)
    toggle.Position = UDim2.new(1, -54, 0.5, -11)
    toggle.BackgroundColor3 = default and Color3.fromRGB(255, 50, 50) or Color3.fromRGB(50, 25, 25)
    toggle.BorderSizePixel = 0
    toggle.Text = ""
    toggle.AutoButtonColor = false
    toggle.Parent = row

    local tc = Instance.new("UICorner")
    tc.CornerRadius = UDim.new(1, 0)
    tc.Parent = toggle

    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 18, 0, 18)
    knob.Position = default and UDim2.new(1, -20, 0.5, -9) or UDim2.new(0, 2, 0.5, -9)
    knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    knob.BorderSizePixel = 0
    knob.Parent = toggle

    local kc = Instance.new("UICorner")
    kc.CornerRadius = UDim.new(1, 0)
    kc.Parent = knob

    local state = default
    toggle.MouseButton1Click:Connect(function()
        state = not state
        Tween:Create(toggle, TweenInfo.new(0.2), {
            BackgroundColor3 = state and Color3.fromRGB(255, 50, 50) or Color3.fromRGB(50, 25, 25)
        }):Play()
        Tween:Create(knob, TweenInfo.new(0.2), {
            Position = state and UDim2.new(1, -20, 0.5, -9) or UDim2.new(0, 2, 0.5, -9)
        }):Play()
        if callback then callback(state) end
    end)
end
